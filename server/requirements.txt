fastapi>=0.138.2,<0.139.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
httpx2>=2.0.0  # resolves the Starlette TestClient deprecation warning (see commit 770de44); not imported directly
python-dotenv>=1.0.0
cryptography>=44.0.0
curl-cffi>=0.8
aiohttp>=3.10.0
# hermes-id agent auth — main.py imports hermes_id.fastapi_plugin at
# startup; missing this dep breaks the whole backend on a clean install
# (and CI: `pip install -r requirements.txt` + pytest → ImportError).
# Installed from the source repo (NOT published to PyPI) — CI runners and
# the Docker build both resolve it via git+https.
hermes-id @ git+https://github.com/omiinaya/hermes-auth-plugin.git@2d4a3cdfd92599e3a16ad2c18fa8467c35e8c2ca
